<%= form_for(@email_list_type) do |f| %>
  <% if @email_list_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email_list_type.errors.count, "error") %> prohibited this email_list_type from being saved:</h2>

      <ul>
      <% @email_list_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ListType %><br>
    <%= f.text_area :ListType %>
  </div>
  <div class="field">
    <%= f.label :ListTypeDescription %><br>
    <%= f.text_area :ListTypeDescription %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
