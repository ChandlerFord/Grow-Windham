<%= form_for(@organization) do |f| %>
  <% if @organization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organization.errors.count, "error") %> prohibited this organization from being saved:</h2>

      <ul>
      <% @organization.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :OrganizationName, "Organization Name" %><br>
    <%= f.text_area :OrganizationName %>
  </div>
  <div class="field">
    <%= f.label :OrganizationType, "Organization Type" %><br>
    <%= f.select :OrganizationType, options_for_select(@organization_types.map{ |c| [c.OrganizationType] }), {include_blank: true} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
