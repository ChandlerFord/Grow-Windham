<%= form_for(@event_role) do |f| %>
  <% if @event_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_role.errors.count, "error") %> prohibited this event_role from being saved:</h2>

      <ul>
      <% @event_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :EventRole %><br>
    <%= f.text_area :EventRole %>
  </div>
  <div class="field">
    <%= f.label :Description %><br>
    <%= f.text_area :Description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
