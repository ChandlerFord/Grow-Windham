<%= form_for(@people_organization) do |f| %>
  <% if @people_organization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@people_organization.errors.count, "error") %> prohibited this people_organization from being saved:</h2>

      <ul>
      <% @people_organization.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :PersonalID, "Personal ID" %><br>
    <%= f.select :PersonalID, @people.map { |p| ["#{p.Firstname} #{p.LastName}", p.id] }, {:prompt => 'Select Person'} %>
  </div>
  <div class="field">
    <%= f.label :OrganizationName, "Organization Name" %><br>
    <%= f.select :OrganizationName, options_for_select(@organization.map{ |c| [c.OrganizationName] }), {include_blank: true} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
