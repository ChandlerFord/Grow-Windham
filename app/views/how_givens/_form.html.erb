<%= form_for(@how_given) do |f| %>
  <% if @how_given.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@how_given.errors.count, "error") %> prohibited this how_given from being saved:</h2>

      <ul>
      <% @how_given.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :HowGiven, "How Given" %><br>
    <%= f.text_area :HowGiven %>
  </div>
  <div class="field">
    <%= f.label :HowGivenDescription, "How Given Description" %><br>
    <%= f.text_area :HowGivenDescription %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
