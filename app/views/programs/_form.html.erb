<%= form_for(@program) do |f| %>
  <% if @program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@program.errors.count, "error") %> prohibited this program from being saved:</h2>

      <ul>
      <% @program.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ProgramName, "Program Name" %><br>
    <%= f.text_area :ProgramName %>
  </div>
  <div class="field">
    <%= f.label :ProgramStartDate, "Program Start Date" %><br>
    <%= f.date_select :ProgramStartDate %>
  </div>
  <div class="field">
    <%= f.label :ProgramEndDat, "Program End Date" %><br>
    <%= f.date_select :ProgramEndDate %>
  </div>
  <div class="field">
    <%= f.label :ProgramDescription, "Program Description" %><br>
    <%= f.text_area :ProgramDescription %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
