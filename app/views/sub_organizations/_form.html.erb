<%= form_for(@sub_organization) do |f| %>
  <% if @sub_organization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_organization.errors.count, "error") %> prohibited this sub_organization from being saved:</h2>

      <ul>
      <% @sub_organization.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :OrganizationName, "Organization Name" %><br>
    <%= f.select :OrganizationName, options_for_select(@organizations.map{ |c| [c.OrganizationName] }), {include_blank: true} %>
  </div>
  <div class="field">
    <%= f.label :SubOrganizationNamem, "Sub-Organization Name" %><br>
    <%= f.text_area :SubOrganizationName %>
  </div>
  <div class="field">
    <%= f.label :Description, "Description" %><br>
    <%= f.text_area :Description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
